{
  "name": "@yoyo/pi-commandcode-provider",
  "version": "0.1.0-local.3",
  "description": "Pi Direct Adapter for Command Code. Credential ownership stays in Pi global models.json.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*"
  },
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --experimental-strip-types --test test/*.test.mjs"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/yoyooyooo/commandcode-bridge.git"
  }
}
